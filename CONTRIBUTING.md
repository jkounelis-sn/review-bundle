# Contributing

- Run `make test` before opening a PR.
- The vendored dataset under `deps/corpus/` is *not* edited here — request
  changes against the upstream corpus repo and then run `make sync-corpus`.
